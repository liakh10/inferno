/* Written once after /deploy. The vault address is read by the site and by the API, so both agree on where prompts are
   paid. Empty until the vault is deployed: launching still works, prompts and burns stay closed. */
export const VAULT = '';
